{
  "name": "diceware",
  "version": "1.0.0",
  "description": "",
  "main": "elm.js",
  "dependencies": {},
  "devDependencies": {
    "chokidar-cli": "^1.2.0",
    "elm": "^0.18.0"
  },
  "scripts": {
    "watch": "chokidar '**/*.elm' -c 'elm-make ./src/Main.elm --output ./public/main.js' --initial",
    "deploy": "git subtree push --prefix public origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "elm-make src/Main.elm --output=./public/main.js"
  },
  "author": "",
  "license": "MIT"
}
